- PARSING
	Verify errors: (provide error messages)
		- file not found
		- file must contain all mandatory in  (KEY=VALUE) format
		- start and exit must be diffent
		- start and exit must be on maze bounds
	Ignore lines starting with '#'

- MAZE GENERATION
	- It must be a class 'MazeGenerator' in a standalone module
	- The 'MazeGenerator' class must have a docstring documenting:
		- how to instantiate and use generator with at least one exemple
		- hot to pass custom paramas it has
		- how to acess the maze and at least one solution

	- Must contain external border
	- Cannot have areas with 3x3 or more
	- Corridor cannot be wider than 2 cells
	- Must have the '42' with walls (not required in small mazes)
	- Create a maze saver in destination file

- OUTPUT REQUIREMENTS
	- file must be written with hex values 0,1,...,F
	- AFTER AN EMPTY LINE -> print the start and exit and shortest valid path

- RENDERING
	- Show clearly the start and exit
	Events needed:
		- Re-generate maze
		- Show/Hide the valid shortest path
		- Change maze walls colors
		- Set the '42' color

- README
	The same Documentation of the mazeGenerator must be on readme
